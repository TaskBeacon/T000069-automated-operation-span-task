Create a landscape TaskBeacon scientific timeline diagram on white. Leave the top 18% completely blank for postprocessed header, with no generated title, logo, brand, or watermark. Two rows labeled MATH PRACTICE and COMBINED SET. Math practice: gray screen with `(3 x 2) + 1 = ?` and SPACE WHEN SOLVED -> gray screen `(3 x 2) + 1 = 7` with `F FALSE   J TRUE` -> small note `deadline = mean RT + 2.5 SD`. Combined set: the same operation screen -> TRUE/FALSE screen -> a single large letter `P` with exact timing `800 ms` -> loop annotation `repeat 3-7 times` -> recall screen with `F H J K L N / P Q R S T Y` and `ORDERED RECALL` -> feedback `Letters 4/5   Math 5/5`, exact timing `2000 ms`. Thin arrows, compact labels, no people/devices/scenery, readable text, consistent gray participant screens.
